{
  "name": "botanica-around-me",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run build:assets && python server.py",
    "test": "npm run build:assets && node --test tests/*.test.mjs && python3 -m unittest discover -s tests",
    "build:assets": "node scripts/build-assets.mjs",
    "build": "node scripts/build-hosted.mjs"
  }
}
